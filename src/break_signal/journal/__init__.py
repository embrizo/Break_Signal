"""Trade journal: SQLite log of signals, trades, tags and outcomes.

Everything the AI coach later reasons about lives here. ``analytics`` is the
only place metrics are computed; ``db`` only stores and retrieves.
"""
