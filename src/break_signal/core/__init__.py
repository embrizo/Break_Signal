"""Core algorithm: pivots, trendlines, breakout detection, state.

This package is a faithful port of ``pine/break_signal.pine`` so the Python
service produces the same signals as the TradingView indicator on the same
candles. Any change here must be mirrored there and vice versa.
"""
